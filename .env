# ============================================================
# Hytale Server - Environment Configuration
# ============================================================

# Java Memory Settings
JAVA_MIN_RAM=3G
JAVA_MAX_RAM=4G

# Server Settings
SERVER_PORT=5520
SERVER_BIND=0.0.0.0

# Backup Settings
ENABLE_BACKUP=true
BACKUP_FREQUENCY=30

# Auth Mode: authenticated (default) oder offline (für LAN-only)
# AUTH_MODE=offline

# Timezone
TZ=Europe/Berlin

# User/Group IDs (match your host user for file permissions)
PUID=1000
PGID=1000
